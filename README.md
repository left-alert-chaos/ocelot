# Ocelot (`rust` branch)
Ocelot is a chess bot that is decidedly not sophisticated.

This is the `rust` branch, where I'm working on porting the existing engine to a faster language. The `main` branch is more complete, but still pretty buggy, so go there if you want to use the engine.
